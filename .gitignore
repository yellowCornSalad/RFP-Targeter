__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.local

# Editor
.vscode/
.idea/
*.swp
*.swo

# Local data (never commit)
data/*.db
data/*.db-journal
data/attachments/
# 회사 기밀 원본 (제안서·특허·인증) — 절대 commit 금지
data/raw/
drafts/
templates/*/
!templates/.gitkeep

# Config secrets
config/profile.yaml
config/secrets.yaml
config/*.local.yaml

# Local-only deploy artifacts (one-time copy)
profile_b64.txt
streamlit_secrets.toml

# Build
build/
dist/
# 정적 사이트 빌드 출력 — GitHub Actions가 gh-pages로 push (인라인 주석은 gitignore 미지원 → 별도 줄)
site/
*.egg-info/

# 회사 기밀 문서 — PUBLIC repo 노출 방지 (특허·인증 정제본, 합격 제안서 패턴, Proposal Builder 설계)
docs/company_assets.md
docs/proposal_builder_design.md
docs/proposals/

# OS
.DS_Store
Thumbs.db
desktop.ini
