{
  "name": "rfp-monitor-trigger",
  "version": "1.0.0",
  "description": "Cloudflare Workers cron — RFP-Targeter monitor 자동 trigger",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
